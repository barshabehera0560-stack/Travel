# Wanderly Backend API - Environment Variables Example
PORT=5000
NODE_ENV=production

# Remote Neon PostgreSQL Connection String (Pooled)
DATABASE_URL="postgresql://username:password@ep-divine-firefly-b3ekdshz-pooler.c-4.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# JWT Secret for User & Admin Authentication
JWT_SECRET="wanderly_super_secure_jwt_secret_key_2026_dev_env"
JWT_EXPIRES_IN="7d"

# Allowed CORS Origins (set to your Vercel deployment URL or * for public API)
CORS_ORIGIN="*"
