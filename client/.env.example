# Wanderly Client - Environment Variables Example
# If deploying fullstack unified on Vercel (monorepo), leave this blank/unset so it defaults to same-domain /api.
# If deploying frontend to Vercel and backend to a separate service (e.g. Render/Railway), set your backend URL:
# VITE_API_URL=https://your-wanderly-backend.onrender.com
